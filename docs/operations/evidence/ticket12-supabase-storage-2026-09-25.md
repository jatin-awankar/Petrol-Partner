# Ticket 12 Supabase Storage rehearsal — 2026-09-25

This supersedes the access state in [the earlier gate inventory](ticket12-provider-gate-2026-09-25.md). The existing `petrol-partner-pilot-baseline` project showed **Unhealthy** in the Supabase dashboard. A dashboard restart returned to **Unhealthy**. Its PostgreSQL pooler was reachable; the project API hostname still returned `ENOTFOUND` through this workstation's default DNS. Public resolvers returned Cloudflare addresses, so the rehearsal used a per-process HTTPS DNS lookup override (`PILOT_STORAGE_DNS_IP`) while retaining the project hostname for TLS. No system DNS settings were changed.

The private `pilot-student-evidence` bucket was created in the existing project. A server-key Storage API read returned `public: false`, `file_size_limit: 524288`, and allowed MIME types `application/pdf`, `image/jpeg`, and `image/png`. The dashboard showed zero bucket policies. Credentials remain in an ignored local environment file.

The checked-in `scripts/student-evidence-storage-rehearsal.mjs` used one fabricated PDF object. It uploaded at **2026-09-25 16:56:24 UTC** and completed deletion checks at **16:56:29 UTC**. The object key was `9a4aabab-2701-4e38-bcf7-580fe2ed4f0e`; SHA-256 of its bytes was `a1bcbdf92858f3252692107c345fb913678e9a527a52d13f94edfd4e46c69829`. Authenticated download and a 60-second signed URL returned the uploaded bytes. A public URL returned HTTP 400. The bucket-level `DELETE /storage/v1/object/pilot-student-evidence` request with the exact key returned one removed object. After deletion, authenticated object read, signed URL read, and object metadata lookup each returned HTTP 400; listing that key returned no object. Supabase's missing-object response body identifies `not_found` with `statusCode: "404"` despite the HTTP 400 response. The API adapter now maps that specific response to a missing object for lost-document re-upload.

After tightening failure cleanup reporting, a second rehearsal used object key `c1aa35a8-5f83-4a27-a15d-624335963920` from **17:00:59.362** to **17:01:02.684 UTC**. It passed the same access, deletion, and empty-list checks. The script prints the key before upload so a failed cleanup can be located.

This establishes immediate Storage API access removal for a synthetic object. It does **not** establish whether internal replicas, backups, object versions, CDN caches, or temporary previews retain bytes, nor a provider-side orphan-object lifecycle. The 60-second signed URL was checked after deletion, but natural expiry and geographically separate caches were not measured. A provider retention statement or further provider-side evidence is required before a permanent-erasure claim or `PILOT_EVIDENCE_PROVIDER_VERIFIED=true`. The project health and workstation DNS issue also need an operational resolution before application-level staging intake. No real student evidence was uploaded and real intake remains closed.

The independent B2 student-review receipt prefix still lacks a verified lifecycle and live credentials. `PILOT_STUDENT_REVIEW_RECEIPT_RETENTION_VERIFIED` remains unset. The receipts contain identity/enrollment snapshots needed for recovery, so the pilot's 90-day personal-data limit requires a documented and rehearsed expiration or deletion path for every version.

Follow-up dashboard inspection still showed the project status as **Unhealthy** after the successful Storage API rehearsal. This is an operational launch gate separate from the Storage object behavior demonstrated above. Backblaze's [lifecycle documentation](https://www.backblaze.com/docs/cloud-storage-lifecycle-rules) says all file versions are retained by default and that Object Lock prevents lifecycle deletion until the lock expires. A B2 bucket and configured prefix must therefore be inspected and rehearsed before the review-receipt retention flag can be set. No B2 credentials or bucket settings are currently available in the ignored local configuration.
