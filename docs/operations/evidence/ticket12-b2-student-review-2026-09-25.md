# Ticket 12 B2 student-review receipt rehearsal — 2026-09-25

The existing private B2 receipt bucket and scoped writer/recovery-reader keys from ticket 09 were reused. Credentials stayed in ignored, mode-0600 local files. Read-only S3 calls returned HTTP 200 for bucket access, AES-256 default encryption, and enabled Object Lock. The lifecycle configuration contained an enabled `ticket11synthetic/` rule that hides current versions after 31 days and deletes noncurrent versions after one day. It contained no rule for `pilot-ticket09/receipts/student-review/`. A version listing for that real review prefix returned zero versions and zero delete markers.

The checked-in `scripts/student-review-b2-rehearsal.ts` requires explicit `PILOT_B2_LIVE_TEST=true`, verifies the bounded synthetic lifecycle, and writes only fabricated data beneath `ticket11synthetic/ticket12/<random UUID>/receipts/student-review/`. The run completed at **2026-09-25 17:17:03 UTC** under prefix `ticket11synthetic/ticket12/749093e7-f02f-4a9b-9395-6f08120e02c0`. The application `B2ReceiptStore` appended one signed student-review receipt, accepted an identical retry without creating a second version, and listed the same payload. A separate recovery-reader key listed exactly one version, downloaded its bytes with AES-256 reported, and read governance Object Lock retention extending at least 29 days from the check. A prior one-off run at **17:15:50 UTC** passed the same checks under prefix `ticket11synthetic/ticket12/f41b01a8-2681-44d2-a60c-bb09c3bb55bb`. These tiny synthetic versions remain under the existing 31-day lifecycle and 30-day lock; they contain no real student information.

This demonstrates the live B2 receipt adapter and a separate reader. It does not establish the lifecycle of real student-review receipts. The current production prefix has no matching rule, and the application verifies every acknowledged review receipt on each protected review. Deleting older receipts without a bounded recovery design would fail that check and restrict protected writes. A real-prefix lifecycle, recovery horizon, unresolved-case hold policy, and expiry rehearsal must be decided and implemented together before setting `PILOT_STUDENT_REVIEW_RECEIPT_RETENTION_VERIFIED=true`. No real intake or deployment was enabled.

Provider references: [B2 lifecycle rules](https://www.backblaze.com/docs/cloud-storage-lifecycle-rules), [Object Lock](https://www.backblaze.com/docs/cloud-storage-object-lock), and [S3 lifecycle read mapping](https://www.backblaze.com/apidocs/s3-get-lifecycle-configuration).
