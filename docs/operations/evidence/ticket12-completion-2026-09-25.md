# Ticket 12 completion evidence — 2026-09-25

The local issue tracker records ticket 12 as **resolved** for the student and adult eligibility review feature slice. The tracked branch is `codex/12-student-adult-eligibility`. Ticket 10, its only dependency, is resolved.

The API and UI support enrollment and redacted age evidence submission, current student status, and MFA-authorized operator approval or rejection. Protected review commits the adult decision, reason, reviewer, audit and recipient notification work, with an idempotency key and independent signed recovery receipt. Raw bytes live outside ordinary PostgreSQL backups and audit payloads. Five-minute single-use grants protect operator document reads; the client revokes preview URLs. The worker schedules decided raw evidence for Storage API deletion at day six, retries failure, records non-sensitive outcomes, respects expiring holds and exposes overdue work. Lost unreviewed evidence can be uploaded again. Approval does not grant phone, driver or car eligibility.

The PostgreSQL HTTP integration suite covers access denial, expired and used grants, decision interruption/retry, idempotency, audit, notification and recovery. Worker PostgreSQL tests cover due deletion, active holds, retry and overdue outcomes. The full repository suite passed: 27 root tests (four skipped), 70 API tests (one skipped), and 13 worker tests; typecheck and build passed. Lint passed with zero errors and ten existing warnings. The [live synthetic Supabase rehearsal](ticket12-supabase-storage-2026-09-25.md) proved immediate API access removal after exact object deletion. The [live synthetic B2 rehearsal](ticket12-b2-student-review-2026-09-25.md) proved one encrypted, locked review receipt and independent-reader recovery under a bounded synthetic prefix.

**This resolves the feature slice, not the real-intake or permanent-erasure gates.** `PILOT_EVIDENCE_PROVIDER_VERIFIED` and `PILOT_STUDENT_REVIEW_RECEIPT_RETENTION_VERIFIED` remain unset. No real documents were ingested and no permanent-erasure claim is accepted. Ticket 28 owns pilot-wide provider/internal retention, the real B2 receipt lifecycle, account-closure deletion and restoration-safe deletion receipts; ticket 32 owns launch approval. The Supabase project still reports Unhealthy. Real intake stays closed until those separate gates are evidenced.
